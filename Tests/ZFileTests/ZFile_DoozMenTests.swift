import XCTest
@testable import ZFile_DoozMen

final class ZFile_DoozMenTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ZFile_DoozMen().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
